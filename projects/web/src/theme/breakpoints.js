/**
 * Viewport width breakpoints in pixels.
 *
 * The breakpoint values were extracted from Bootstrap 4.
 */

const xs = 0;
const sm = 576;
const md = 768;
const lg = 992;
const xl = 1200;

export default {
  xs,
  sm,
  md,
  lg,
  xl,
};
